//  variables
let variable_loca = 0; 
const variables_constantes = 0;
// string (cadenas de texto), int (numeros normales), float (decimales), bools(false, true), list,dicts (clave y valor)
const lista = [0, 1, 2, 3, 4]; // los indices empiezan desde 0
const diccionario = {nombre: "edizon"}; //(clave: "valor")
lista[3];
const valor_clave = diccionario.nombre;
// siempre en react las variables tienen que ser constantes

// flujo de control
const v = true;
const v_f = v ? "verdadero" : "falso";
if(condicional) {
    // la condicion a cumplir
}
else if (condicional) {
    // la condicion a cumplir
}
else {

}
// mostrar mensaje en consola del navegador
console.log("hola");
for (let index = 0 /* variable inicial */; index < lista.length /*condicion*/; index++ /* incremento */) {
    const element = lista[index];
    // lo que hace el bucle
}

while (condicion) {
    // si la condicion no se cumple el bucle continua
}

// funciones
export default function nombreDeLaFuncion(/* aqui van los parametros */) {
    // aqui va el codigo de funcionamiento de la funcion
    return 0;
}


//componente react primera en mayúscula
export default function ComponetenDeReact(){
ret
}

const funcionFlecha = (/* aqui van los parametros */) => {
// aqui va la funcionalidad
};


ComponetenDeReact(() => {

})